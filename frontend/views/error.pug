extends base

block content
    h1.ui.header= title
    p= message
    p For the geek in you, the error returned from the server is:
    code= error

block settings
    .divider
    a(href='/search').item Find Proteins in Experiments
    a(href='/experiments').item Search Experiments
    a(href='/curves').item Analyze

block scripts
    - var delta = 50
